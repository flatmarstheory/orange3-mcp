services:
  orange3-mcp-web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - OPENAI_MODEL=${OPENAI_MODEL:-gpt-4o-mini}
      - ORANGE3_MCP_DATA_DIR=/data
    volumes:
      - ./data:/data
    restart: unless-stopped
